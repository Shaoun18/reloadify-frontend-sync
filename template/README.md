# Template Folder

This folder is reserved for template files and custom page builders support.

## Structure
- `custom-templates/` - Future custom page template handlers
- `builder-integrations/` - Specific page builder integration templates

## Performance Notes
Templates are lazy-loaded on demand to keep the plugin load time minimal for frontend visitors.
